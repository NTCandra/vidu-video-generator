{
  "_meta": {
    "workflow": "i2v_wan22",
    "version": "1.0.0",
    "description": "Image-to-video using Wan 2.2 I2V via ComfyUI (Phase 6+). Requires ComfyUI-VideoHelperSuite.",
    "parameterized_nodes": {
      "load_image": {"field": "image", "param": "{{input_image_path}}"},
      "prompt": {"field": "text", "param": "{{motion_prompt}}"},
      "sampler": {"seed": "{{seed}}", "steps": "{{steps}}", "cfg": "{{cfg}}"}
    },
    "_note": "Full workflow JSON to be filled in during Phase 6 after Wan 2.2 ComfyUI node is available."
  }
}
